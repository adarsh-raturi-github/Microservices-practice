apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-mongo
  template:
    metadata:
      labels:
        app: auth-mongo
    spec:
      containers:
        - name: auth-mongo-container
          image: mongo


## cluster ip service
---
apiVersion: v1
kind: Service
metadata:
  name: auth-mongo-cluster-ip-srv
spec:
  selector:
    app: auth-mongo
  ports:
    - name: auth-mongo-port
      protocol: TCP
      port: 27017
      targetPort: 27017
